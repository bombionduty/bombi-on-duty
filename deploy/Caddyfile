# Caddy auto-HTTPS. {$DOMAIN} comes from the DOMAIN env var (your nip.io host).
# Caddy automatically obtains + renews a free Let's Encrypt certificate.
{$DOMAIN} {
	encode gzip
	reverse_proxy app:8000
}
